tesseract = C’est une librairie de OCR (Optical Character Recognition).
jspdf = C’est une librairie pour créer des fichiers PDF en JavaScript.

sh```
        npm install tesseract.js jspdf
```


FONCTIONNALITÉS IMPLÉMENTÉES
        - Caméra plein écran avec mode environment
        - Rectangle de détection avec coins stylisés
        - Animation laser scan pendant l'analyse
        - Scan automatique toutes les 3 secondes
        - OCR en temps réel avec Tesseract.js (langue française)
        - Analyse IA du texte (type document, résumé, extraction infos)
        - Export PDF automatique avec image + texte
        - Notifications toast pour chaque étape
        - Navigation vers /clonea/stack/analysis avec state
        - Bouton capture manuelle (fallback)
        - Design mobile-first ultra pro